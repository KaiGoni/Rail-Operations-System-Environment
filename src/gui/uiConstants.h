#ifndef RAIL_OPERATIONS_SYSTEM_ENVIRONMENT_UICONSTANTS_H
#define RAIL_OPERATIONS_SYSTEM_ENVIRONMENT_UICONSTANTS_H

#include <SFML/Graphics/Color.hpp>

const float horizontalPadding = 10;
const float verticalPadding = 5;

const sf::Color selectedColor = sf::Color(150, 150, 200);
const sf::Color hoveredColor = sf::Color(150, 150, 150);
const sf::Color unselectedColor = sf::Color(100, 100, 100);
const sf::Color dividerColor = sf::Color(100, 100, 100);
const sf::Color textColor = sf::Color(200, 200, 200);

#endif //RAIL_OPERATIONS_SYSTEM_ENVIRONMENT_UICONSTANTS_H