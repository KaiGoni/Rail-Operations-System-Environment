#include "interface.h"

class Panel{
private:
    // sf::rect background;
public:
    Panel() {
        // background.setSize({200, 900});
        // background.setFillColor(sf::Color(50, 50, 50));
    }
};